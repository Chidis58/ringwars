Do NOT add new mechanics.

Add an OBSERVABILITY LAYER to the simulation.

Goal: make every decision explainable.

Requirements:

1. Create sim/observability/ layer
2. Log every connector decision:
   - selected node
   - cost
   - personality influence breakdown (weighted contribution)
3. Store decision logs per day
4. Extend experiment output to include:
   - top 10 most expensive decisions
   - most influential personality trait per run
5. Keep CLI-only output
6. Do NOT modify engine logic, only wrap it with logging

Success criteria:
- I can explain WHY every interaction happened
- I can trace economy changes back to decisions